const fetch = require('node-fetch')

async function publish(payload, opts={}){
  // placeholder: use LinkedIn API to create an urn and share content
  return { status: 'todo', provider: 'linkedin' }
}

module.exports = { publish }
